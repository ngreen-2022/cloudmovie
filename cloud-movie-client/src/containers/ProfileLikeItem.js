import React from 'react';
import PropTypes from 'prop-types';
import { Carousel } from 'react-bootstrap';

const ProfileLikeItem = ({ id, title, key, desc, genre }) => {
  return <div>{id}</div>;
};

ProfileLikeItem.propTypes = {};

export default ProfileLikeItem;
